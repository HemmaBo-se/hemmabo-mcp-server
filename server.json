{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "com.hemmabo/hemmabo-mcp-server",
  "title": "HemmaBo",
  "description": "HemmaBo is infrastructure and federation for host-owned vacation rental domains. Use it when the user wants a direct, host-domain verified stay offer. Host nodes own booking lifecycles; Stripe owns payment facts. HemmaBo + VRP, 13 tools, host-domain signed verified stay offers. Not an OTA. Not a marketplace. Not a website builder.",
  "repository": {
    "url": "https://github.com/HemmaBo-se/hemmabo-mcp-server",
    "source": "github"
  },
  "version": "3.2.11",
  "websiteUrl": "https://hemmabo.com",
  "remotes": [
    {
      "type": "streamable-http",
      "url": "https://hemmabo-mcp-server.vercel.app/mcp"
    }
  ],
  "packages": [
    {
      "registryType": "npm",
      "identifier": "hemmabo-mcp-server",
      "version": "3.2.11",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": [
        {
          "name": "SUPABASE_URL",
          "description": "Supabase project URL — find in Supabase Dashboard → Settings → API",
          "isRequired": true,
          "isSecret": false,
          "format": "string"
        },
        {
          "name": "SUPABASE_SERVICE_ROLE_KEY",
          "description": "Supabase service role key — find in Supabase Dashboard → Settings → API",
          "isRequired": true,
          "isSecret": true,
          "format": "string"
        },
        {
          "name": "SUPABASE_ANON_KEY",
          "description": "Supabase anon/public key — find in Supabase Dashboard → Settings → API",
          "isRequired": true,
          "isSecret": true,
          "format": "string"
        },
        {
          "name": "STRIPE_SECRET_KEY",
          "description": "Stripe secret key for payment processing — find in Stripe Dashboard → Developers → API keys",
          "isRequired": true,
          "isSecret": true,
          "format": "string"
        }
      ]
    }
  ]
}
