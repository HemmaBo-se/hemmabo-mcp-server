{
  "name": "hemmabo-mcp-server",
  "version": "3.1.5",
  "description": "Vacation rental booking infrastructure — own domain, Stripe direct payments, 0% commission. Not Airbnb, not Lodgify. 9 production tools: search, availability, pricing, booking, checkout, cancellation, rescheduling, status, modifications. Set it and forget it — AI agents book autonomously.",
  "network_id": "hemmabo_verified",
  "transport": "streamable-http",
  "endpoint": "/mcp",
  "configSchema": {
    "type": "object",
    "properties": {
      "SUPABASE_URL": {
        "type": "string",
        "description": "Supabase project URL (e.g. https://xxx.supabase.co)"
      },
      "SUPABASE_SERVICE_ROLE_KEY": {
        "type": "string",
        "description": "Supabase service role key (secret, full database access)"
      },
      "STRIPE_SECRET_KEY": {
        "type": "string",
        "description": "Stripe secret key for payment processing (required for checkout/cancel/reschedule)"
      }
    },
    "required": ["SUPABASE_URL", "SUPABASE_SERVICE_ROLE_KEY"],
    "additionalProperties": false
  },
  "capabilities": {
    "tools": [
      "search_properties",
      "check_availability",
      "get_canonical_quote",
      "create_booking",
      "negotiate_offer",
      "checkout",
      "cancel_booking",
      "get_booking_status",
      "reschedule_booking"
    ]
  }
}
